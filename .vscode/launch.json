{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "superset-provision (uv)",
      "type": "debugpy",
      "request": "launch",
      "module": "superset_provision.cli",
      "python": "${workspaceFolder}/.venv/bin/python",
      "cwd": "${workspaceFolder}",
      "args": [
        "--vars", ".env"
      ],
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": false
    }
  ]
}
